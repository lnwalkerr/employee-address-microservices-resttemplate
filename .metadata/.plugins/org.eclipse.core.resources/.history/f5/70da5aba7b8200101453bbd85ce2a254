package com.walker.microservices.address.service;
import org.modelmapper.ModelMapper;
import org.springframework.stereotype.Service;

import com.walker.microservices.address.dto.AddressResponse;
import com.walker.microservices.address.model.Employee;
import com.walker.microservices.address.repository.EmployeeRepository;

import lombok.AllArgsConstructor;

@AllArgsConstructor
@Service
public class EmployeeServiceImpl implements AddressService {
	ModelMapper mapper;
	EmployeeRepository employeeRepository;

	public AddressResponse getEmployeeById(Long id) {
		  Employee employee = employeeRepository.findById(id).get();
		  AddressResponse employeeResponse = mapper.map(employee, AddressResponse.class);
		return employeeResponse;
	}

	

}
